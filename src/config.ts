export const config = {
    splatoonInk: {
        baseUrl: 'https://splatoon2.ink',
        data: {
            schedules: '/data/schedules.json'
        },
        assets: {
            splatnet: '/assets/splatnet'
        }
    }
} 